"use client";
import { useEffect, useState } from "react";
import styles from '../queue.module.css'

export default function Queue({ session }: { session: any }) {
  
  return (
    <div className={styles.Queue}>
        <div></div>
    </div>
  );
}
